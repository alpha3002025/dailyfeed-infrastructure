# NOTE: This file is deprecated and no longer used.
# The ingress-nginx-controller is now deployed with hostPort configuration
# using ingress-nginx-hostport.yaml instead.
#
# See: ingress-nginx-hostport.yaml for the current configuration

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingress-nginx-controller
  namespace: ingress-nginx
spec:
  template:
    spec:
      containers:
      - name: controller
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            memory: 512Mi
